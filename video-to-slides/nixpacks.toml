[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "libGL", "glib"]

[phases.install]
cmds = ["pip install -r requirements.txt gunicorn"]

[start]
cmd = "gunicorn --workers 2 --timeout 300 --bind 0.0.0.0:$PORT app:app"
